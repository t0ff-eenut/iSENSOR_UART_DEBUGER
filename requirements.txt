# iSENSOR UART Debugger - Python dependencies
# 설치: pip install -r requirements.txt

# UI
PyQt6>=6.4.0
pyqtgraph>=0.13.0

# Serial / BLE
pyserial>=3.5
bleak>=0.21.0

# AI / ML
numpy>=1.24.0
scikit-learn>=1.3.0
# torch: 현재 가상환경은 Python 3.14 → CPU 전용 빌드만 가능 (PyPI)
#   GPU(CUDA) 사용하려면 Python 3.12 환경에서 아래 명령으로 설치:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   (PyTorch CUDA 빌드는 Python ≤ 3.12 지원, GTX 1660 / CUDA 드라이버 12.x 이상 환경 확인됨)
torch>=2.0.0
matplotlib>=3.7.0
pandas>=1.5.0

# Vision (웹캠 사람 감지)
opencv-python>=4.8.0
# mediapipe 0.10+ 는 solutions API 가 제거됨 → MediaPipe 백엔드 비활성화 (자동 HOG 폴백)
# 0.9.x 는 PyPI에서 Python 3.10+ 전용 빌드로만 제공 — 현 환경에서 설치 불가
mediapipe>=0.10.0   # 설치된 경우 YOLO/HOG 백엔드 사용 권장
ultralytics>=8.0.0  # YOLOv8 (yolov8n.pt 첫 실행 시 자동 다운로드)
